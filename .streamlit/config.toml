[theme]
# OS 다크 모드와 무관하게 항상 light 테마 적용 (디자인 일관성)
base = "light"
primaryColor = "#667eea"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f7f8fc"
textColor = "#2d3748"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
