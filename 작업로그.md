# Go면접 프로젝트 작업 로그

---

## 2025-05-21

### EC2 배포 (Streamlit 버전)

1. **EC2 인스턴스 접속 성공**
   - 인스턴스: t3.small, Amazon Linux 2023
   - Key Pair: `pj-kmuai-02-key.pem`
   - 접속: `ssh -i ~/Downloads/pj-kmuai-02-key.pem ec2-user@<퍼블릭IP>`
   - 주의: 인스턴스 재시작 시 IP 바뀜 (탄력적 IP 미설정)

2. **Security Group 설정**
   - 22번 포트 (SSH) 열림
   - 8501번 포트 (Streamlit) 열림 - 소스: 0.0.0.0/0

3. **프로젝트 파일 EC2 업로드**
   - `scp -i ~/Downloads/pj-kmuai-02-key.pem -r "/Users/jun/Downloads/Go면접_공유용/" ec2-user@<IP>:~/gofactory`
   - 업로드된 파일: app.py, ai_client.py, config.py, database.py, chat_engine.py, requirements.txt 등

4. **Streamlit 실행**
   - `streamlit run app.py --server.port 8501 --server.address 0.0.0.0`
   - 사이트 접속: `http://<퍼블릭IP>:8501`

5. **문제 발생 및 해결 시도**
   - Gemini API 키: quota 0 (무료 할당량 소진) → 사용 불가
   - Groq API 키: 한글 인코딩 에러 (`ascii codec can't encode`) → API 키에 한글 문자 혼입 의심
   - `export LANG=en_US.UTF-8`, `export PYTHONIOENCODING=utf-8` 설정했으나 미해결
   - 원인: GROQ_API_KEY 환경변수에 비ASCII 문자 포함된 것으로 확인
   - 해결: 키를 작은따옴표로 감싸서 재설정 필요 `export GROQ_API_KEY='gsk_...'`

---

### 아키텍처 전환 결정

6. **Streamlit → FastAPI + HTML/Tailwind/JS 전환 결정**
   - 이유: 6인 팀 병렬 개발, React 경험자 없음
   - 스택: FastAPI (백엔드) + HTML + Tailwind CSS + vanilla JS (프론트)
   - 팀 분배: 백엔드 3명 + 프론트엔드 3명

---

### 스펙 문서 생성

7. **interview-mode-upgrade 스펙** (나중에 적용 예정)
   - 요구사항 8개: BYO-LLM, 통합 인터페이스, 성격 모드, 형식 모드, 카테고리, 다크모드, 체험모드
   - 설계: Provider 패턴, PromptBuilder, TrialManager
   - 작업 목록: 10개 메인 태스크

8. **fastapi-frontend-migration 스펙** (현재 진행 중)
   - 요구사항 10개: FastAPI 서버, 질문 생성 API, 답변 평가 API, 기록 조회, 통계, 명언, 프론트 페이지들, 헬스체크
   - 설계: FastAPI + uvicorn, 정적 파일 서빙, Pydantic 모델
   - 작업 목록: 11개 메인 태스크, 20개 필수 서브태스크
   - 예상 소요: 2~3주 (6명 병렬)

---

### 현재 진행 상황

- [x] Task 1.1: 프로젝트 디렉토리 구조 생성 — 완료
- [x] Task 1.2: Pydantic 모델 정의 — 완료
- [x] Task 1.3: 환경 설정 모듈 — 완료
- [x] Task 1.4: requirements.txt 및 .env.example 작성 — 완료
- [x] Task 2.1: DatabaseService 구현 — 완료
- [x] Task 3.1: AIService 구현 — 완료
- [x] Task 4.1: Interview 라우터 — 완료
- [x] Task 4.2: Records 라우터 — 완료
- [x] Task 4.3: Health 라우터 — 완료
- [x] Task 5.1: FastAPI 앱 팩토리 (main.py) — 완료
- [x] Task 7.1: 메인 HTML 페이지 — 완료
- [x] Task 7.2: 커스텀 CSS (다크모드 포함) — 완료
- [x] Task 8.1: 앱 초기화 JS 모듈 — 완료
- [x] Task 8.2: 면접 진행 JS 모듈 — 완료
- [x] Task 8.3: 기록/통계 JS 모듈 — 완료
- [ ] 테스트 작성 (선택사항)
- [ ] README.md 작성
- [ ] EC2 배포

---

### 메모

- EC2 퍼블릭 IP: 재시작 시 변경됨 (마지막 확인: 18.209.50.8)
- Groq 키 발급: https://console.groq.com/keys
- Gemini 키 발급: https://aistudio.google.com/apikey (현재 quota 문제)
- 탄력적 IP 할당하면 IP 고정 가능 (아직 미설정)
