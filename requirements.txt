# ===========================================
# Go면접 FastAPI 서버 - Python Dependencies
# ===========================================
# Install: pip install -r requirements.txt

# --- FastAPI Backend ---
fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic-settings==2.6.1
python-dotenv==0.21.0

# --- AI Providers ---
# v4: OpenRouter(BYOK) 제거. Bedrock 단일 + MCP 서버로 사용자 측 LLM 일원화.
google-generativeai==0.8.6      # legacy: 음성 전사 등 멀티모달 폴백 (Streamlit 폐기 시 제거 예정)
anthropic[bedrock]>=0.46.0      # AWS Bedrock + Anthropic 공식 SDK
boto3>=1.36.0                   # Bedrock IAM 인증 (EC2 Role 자동 주입)

# --- Streamlit Frontend (legacy / 점진적 마이그레이션 중) ---
streamlit
streamlit-mic-recorder

# --- Company Info Crawler (Phase 2 PoC) ---
beautifulsoup4>=4.12
lxml>=5.0
# requests: openai SDK가 transitive로 가져옴, 명시 추가는 생략

# --- MCP Server (Phase 4 — Claude Desktop 사용자 구독 활용) ---
fastmcp>=2.0

# --- Testing ---
pytest==7.4.4
httpx==0.27.0
hypothesis==6.152.9
